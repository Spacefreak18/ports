# description	: subversion version control
# depends	: apr apr-util sqlite zlib lz4 openssl utf8proc

name=subversion
version=1.14.1
release=1
source="https://dlcdn.apache.org/$name/$name-$version.tar.gz"

build() {
	cd $name-$version

	./configure --prefix=/usr
	make
	make DESTDIR=$PKG install
}
